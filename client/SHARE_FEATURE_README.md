# Share Feature - Frontend Components

## Overview
This document describes the UI components for the Share feature, which allows users to share files and folders with other users via email or shareable links.

## Components Created

### 1. ShareModal.jsx
The main modal component that orchestrates the sharing UI.





















































































































































































































































```/>  // ... other handlers  onShareUser={handleShareUser}  userRole={user.role}  onClose={handleCloseShareModal}  item={shareItem}<ShareModal// ShareModal is shown/>  // ... other props  userRole={user.role}  onShare={(item, type) => handleOpenShareModal(item, type)}  baseUrl={BASE_URL}  file={file}<FileRow// When Share button is clicked```jsx## Usage Example- `src/DirectoryUI.jsx`- `src/components/DirectoryRow.jsx`- `src/components/FileRow.jsx`✅ Updated:- `src/components/LinkShareSection.jsx`- `src/components/SharedUserRow.jsx`- `src/components/ShareModal.css`- `src/components/ShareModal.jsx`✅ Created:## File Checklist7. **Advanced Features**: Consider adding expiration dates, custom messages, or access logs6. **Notifications**: Add toast notifications for successful operations5. **Real-time Updates**: Consider adding WebSocket support for live share updates4. **Loading States**: Add loading indicators during async operations3. **Error Handling**: Add proper error messages and validation2. **Replace Placeholder Handlers**: Update the handler functions in DirectoryUI.jsx to call actual backend endpoints1. **Backend Implementation**: Create the endpoints listed above## Next Steps```// Click Share button to see the modal with this data};  }    permission: "viewer"    token: "abc123",    enabled: true,  linkShare: {  ],    { userId: "user2", email: "bob@example.com", permission: "editor" }    { userId: "user1", email: "alice@example.com", permission: "viewer" },  sharedWith: [  name: "Test Document.pdf",  _id: "file1",const mockFile = {// In DirectoryUI.jsx, add mock data to files/folders```javascriptSince backend integration is pending, you can test the UI with mock data:## Testing the UI- Follows the same button and input styling conventions- Color scheme matches the app: `#0078d4` for primary actions, `#dc2626` for danger actions- Responsive design with mobile breakpoints at 767px- Uses existing CSS classes from App.css where possible- All components follow the Windows Explorer theme used throughout the app## Styling Notes   - Returns: `{ sharedWith: [], linkShare: {} }`7. **GET /api/share/:itemId** - Get sharing details for an item   - Returns: Updated link share object   - Body: `{ permission }`6. **PATCH /api/share/link/:itemId** - Update link permission   - Returns: Success message5. **DELETE /api/share/link/:itemId** - Disable link sharing   - Returns: Link share object with token   - Body: `{ itemId, permission }`4. **POST /api/share/link** - Enable link sharing   - Returns: Success message3. **DELETE /api/share/:shareId** - Remove user access   - Returns: Updated share object   - Body: `{ permission }`2. **PATCH /api/share/:shareId** - Update share permission   - Returns: Updated sharedWith array   - Body: `{ itemId, email, permission }`1. **POST /api/share** - Share item with userThese are the endpoints that need to be created to make the feature fully functional:## Backend Endpoints to Implement```}  role: "Owner" // or "Admin", "Editor", "Viewer", "User"  email: "john@example.com",  name: "John Doe",  _id: "user123",{```javascript### User Object```}  }    permission: "viewer" // or "editor"    token: "abc123xyz",    enabled: true,  linkShare: {  ],    }      permission: "viewer" // or "editor"      email: "user@example.com",      userId: "user123",      _id: "share1",    {  sharedWith: [  name: "document.pdf",  _id: "file123",{```javascript### File/Folder Object## Expected Backend Data Structure- Do not see the Share button at all### Other Roles (Editor, Viewer, User)- All inputs and action buttons are disabled- **Cannot** make any changes (read-only)- Can view all sharing information### Admin Role- Can change link permissions- Can enable/disable link sharing- Can change user permissions- Can add/remove users- Can view all sharing information### Owner Role## Permission SystemThe ShareModal is rendered conditionally when `shareModalOpen` is true, and receives all necessary props including the current user's role.### Modal Integration6. `handleUpdateLinkPermission(permission)` - Update link permission5. `handleDisableLinkShare()` - Disable link sharing4. `handleEnableLinkShare(permission)` - Enable link sharing3. `handleRemoveShare(userId)` - Remove user access2. `handleUpdateShare(userId, newPermission)` - Update user permission1. `handleShareUser(email, permission)` - Share with a new userAll handlers are currently placeholder functions that log to console and update local state. These need to be connected to your backend endpoints:### Handler Functions (Placeholder Implementation)```const [shareItemType, setShareItemType] = useState(null);const [shareItem, setShareItem] = useState(null);const [shareModalOpen, setShareModalOpen] = useState(false);```javascript### State Added## Integration in DirectoryUI.jsx- Share button appears after Delete button- Added Share button visible only for Owner and Admin roles**Changes:**- `userRole` - Current user's role to determine button visibility- `onShare(directory, "folder")` - Callback when Share button is clicked**New Props:**### DirectoryRow.jsx- Share button appears after Delete button, before Download- Added Share button visible only for Owner and Admin roles**Changes:**- `userRole` - Current user's role to determine button visibility- `onShare(file, "file")` - Callback when Share button is clicked**New Props:**### FileRow.jsx## Updated ComponentsComprehensive styling for all share components following the app's Windows Explorer theme.### 4. ShareModal.css- Update link permission (viewer/editor)- Copy share link to clipboard- Toggle link sharing on/off**Features:**- `isProcessing` - Boolean to disable controls during operations- `onUpdateLinkPermission(permission)` - Callback to update link permission- `onDisableLinkShare()` - Callback to disable link sharing- `onEnableLinkShare(permission)` - Callback to enable link sharing- `canEdit` - Boolean indicating if user can modify link sharing- `linkShare` - Object with `enabled`, `token`, `permission` or null**Props:**Manages link-based sharing UI.### 3. LinkShareSection.jsx- `isProcessing` - Boolean to disable controls during operations- `onRemove(userId)` - Callback to remove user access- `onUpdatePermission(userId, newPermission)` - Callback to update permission- `canEdit` - Boolean indicating if user can modify sharing- `user` - Object with `_id`/`userId`, `email`/`username`, `permission`**Props:**Displays a single shared user with controls.### 2. SharedUserRow.jsx- Permission-based UI (Owner can edit, Admin can only view)- Update permissions for users and links- Enable/disable link sharing- View and manage shared users list- Share with users via email**Features:**- `onUpdateLinkPermission(permission)` - Async handler to update link permission- `onDisableLinkShare()` - Async handler to disable link sharing- `onEnableLinkShare(permission)` - Async handler to enable link sharing- `onRemoveShare(userId)` - Async handler to revoke access- `onUpdateShare(userId, newPermission)` - Async handler to update permission- `onShareUser(email, permission)` - Async handler to share with a user- `userRole` - Current user's role ('Owner', 'Admin', etc.)- `onClose` - Callback to close the modal- `item` - The file or folder object containing `_id`, `name`, `sharedWith`, `linkShare`, etc.**Props:**
